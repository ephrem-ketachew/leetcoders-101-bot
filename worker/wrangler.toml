name = "leetcoders-101-bot"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
GITHUB_REPO = "ephrem-ketachew/leetcoders-101-bot"
# Telegram /today, /stats, /help are disabled by default (daily cron only).
# Set to "true" and redeploy to re-enable on-demand commands.
COMMANDS_ENABLED = "false"
